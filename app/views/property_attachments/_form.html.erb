<%= form_with(model: property_attachment, local: true) do |form| %>
  <% if property_attachment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(property_attachment.errors.count, "error") %> prohibited this property_attachment from being saved:</h2>

      <ul>
      <% property_attachment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :properties %>
    <%= form.text_field :properties %>
  </div>

  <div class="field">
    <%= form.label :photo %>
    <%= form.text_field :photo %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
